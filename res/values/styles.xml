<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppTheme" parent="android:Theme.Light" />

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->

    </style>

    <style name="activity" parent="@android:style/Theme">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <!-- 是否浮现在activity之上    <item name="android:windowIsFloating">true</item> -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <!-- 模糊 <item name="android:backgroundDimEnabled">true</item> -->

    </style>

    <!-- 与顶部标题栏相关的style -->
    <style name="layout_top_title">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/layout_top_height</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:background">@drawable/bg_bar_top</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="btn_left_top_back">
        <item name="android:layout_width">@dimen/btn_width_left_top_back</item>
        <item name="android:layout_height">@dimen/btn_height_left_top_back</item>
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginLeft">@dimen/btn_margin_left_top_back</item>
        <item name="android:background">@drawable/bg_btn_return</item>
        <item name="android:paddingRight">@dimen/btn_padding_right_top_back</item>
        <item name="android:text">@string/back</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/btn_textsize_top_back</item>
    </style>

    <style name="text_top_title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_textsize_top_title</item>
    </style>

</resources>