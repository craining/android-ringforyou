<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 颜色渐变效果 -->
    <gradient
        android:angle="0"
        android:endColor="@color/white"
        android:paddingTop="5dp"
        android:startColor="@color/white" />
    <!-- 按钮四个角的平滑度 -->
    <corners android:radius="5dip" />

    <!-- 字距离按钮的四个边距 -->
    <padding
        android:bottom="7dip"
        android:left="7dip"
        android:right="7dip"
        android:top="7dip" />
    <!-- 按钮的光环的宽度与颜色 -->
    <stroke
        android:width="2dip"
        android:color="@color/login_edit_focused" />

</shape>